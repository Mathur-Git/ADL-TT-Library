# projects/ is UNTRACKED by choice (2026-08-04; the _TEMPLATE exception was removed and
# the whole directory untracked 2026-08-11). This repo is PUBLIC, and projects/ holds
# trading theses, strategy economics and desk knowledge. Nothing in here gets pushed.
# Consequence to be aware of: PROJECT.md files have no version history and no off-machine
# copy, so back them up some other way - a PRIVATE remote or a synced folder, not this repo.
projects/

.obsidian/
.DS_Store
*.log
node_modules/
.env
.env.local

# Working output from `patterns.py --extract`
*.pattern.json

__pycache__/
*.pyc
